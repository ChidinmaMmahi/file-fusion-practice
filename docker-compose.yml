services:
  frontend:
    image: chidinmammahi/file-fusion-frontend:latest
    ports:
      - "8080:80"
    depends_on:
      - backend

  backend:
    image: chidinmammahi/file-fusion-backend:latest
    expose:
      - "3000"
